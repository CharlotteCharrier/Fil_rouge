
spring:
  application:
    name: spring-security
  datasource:
    url: jdbc:postgresql://localhost:5435/ttm_fil_rouge?currentSchema=public
    username: postgres
    password: postgres
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    database: postgresql